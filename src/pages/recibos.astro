---
import Header from '../components/Header.astro';
import Sidebar from '../components/Sidebar.astro';
import Layout from '../layouts/Layout.astro';
---

<Layout title="Recibos de pago">
  {/* This content will be injected into the <slot /> of your Layout.astro component.
    We assume Layout.astro handles the main <html>, <head>, and <body> tags, 
    and applies a base background color like bg-gray-100 to the body or a main wrapper.
  */}
  <div class="flex h-screen overflow-hidden"> {/* Main wrapper for sidebar and content area */}
    
    <Sidebar /> {/* Your Sidebar component. Its internal styling will determine its appearance. */}
    
    <div class="flex-1 flex flex-col overflow-y-auto"> {/* Main content area that allows scrolling */}
      
      <Header /> {/* Your Header component. It should contain user info, etc. */}
      
      {/* Page-specific content for "Recibos de pago" */}
      <main class="p-8 flex-1 bg-gray-100"> {/* Ensures gray background for content area if not set by Layout, and allows content to expand */}
        
        <h1 class="text-2xl font-semibold text-blue-900 flex items-center gap-2">
          <svg class="w-6 h-6 text-blue-900" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
            <path d="M4 3a1 1 0 00-1 1v12a1 1 0 001.447.894L6 16.118l1.553.776a1 1 0 00.894 0L10 16.118l1.553.776a1 1 0 00.894 0L14 16.118l1.553.776A1 1 0 0017 16V4a1 1 0 00-1-1H4z"></path>
          </svg>
          Recibos de pago
        </h1>

        <div class="mt-6 bg-white p-6 rounded-lg shadow-md max-w-xl">
          <p class="text-sm mb-4 text-gray-600">
            A continuación se listan las opciones disponibles para generar el respectivo recibo de pago.
          </p>

          <div class="flex flex-col sm:flex-row gap-4 items-center">
            <div class="w-full sm:flex-auto"> {/* Allows the select input to grow */}
              <select class="border border-gray-300 rounded-md px-4 py-2 w-full focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition">
                <option value="">Seleccione un recibo de pago</option>
                <option value="inscripcion">Inscripción</option>
                <option value="mensualidad">Mensualidad</option>
                {/* Agrega más opciones según sea necesario */}
              </select>
            </div>
            
            <button class="bg-blue-800 text-white px-6 py-2 rounded-md hover:bg-blue-700 transition duration-150 ease-in-out whitespace-nowrap w-full sm:w-auto">
              Generar recibo de pago
            </button>
          </div>
        </div>
        
      </main>
    </div>
  </div>
</Layout>